{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for agentOS GUI",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
